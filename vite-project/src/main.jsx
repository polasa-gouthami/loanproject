import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.css' // Make sure this path points to your Tailwind CSS file
import App from './App'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
